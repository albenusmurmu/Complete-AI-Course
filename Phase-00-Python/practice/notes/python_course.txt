List Method :-
- symbol [] = mutable but not change in original value

methods : 
- .append(20) // this value append in last of numbers
- .insert(0, 20) // we can set the custmize value where we want to insert the value
- .remove(5) // here 5 is a value if we want to remove the number 5 which is available in number variable 
- .clear() // remove all the numbers from the number variable




tuple :- 
- symbol () = immutable can't chnage or reasign value 


Functions :-
 In python, a Functions is a self combined, resuable block of code designed to perform a specific task
 ex :-
 def function_name(parameter1, parameter2):
    # Function body: code that runs when called
    result = parameter1 + parameter2
    return result  # Sends a value back to the caller

 #04
# here I am giving you the brief example for both conditions
# 01 when We're trying to pass the values in the form of keyword and then possitional arguments, and genuinely it's not perfact way to pass the values.
# 02 If we're trying to pass values then first we need to define possitional argument and then keyword argument
# here are both code so that it can be clear
# 01
def user_data(first_name, last_name, age,):
    print(f'Hey, {first_name } {last_name} you are {age} years old')
user_data(first_name = 'John', last_name = 'Doe', age = 18)

# 02
def user_data(first_name, last_name, age,):
    print(f'Hey, {first_name } {last_name} you are {age} years old')
user_data(first_name = 'John', last_name = 'Doe', age = 18)

# 03 return statement
# Here we're experiment with code like when we can't return the values and then try to print the values outside the
# functions it returns the None, but why happen this, I explain this through the code
def mathematical_calculations(a,b):
    print(a*b)
    # skip return statement
# Positional argument
print(mathematical_calculations(25,30)) # this print values return none

